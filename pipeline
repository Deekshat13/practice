pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls'
                sh 'cd "Flask GET Rest API" && ls'
                sh 'docker build -t flask -f Dockerfile .'
            }
        }
        
        // Add more stages as needed
}
}
